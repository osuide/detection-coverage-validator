# OAuth Credentials for Terraform
# Copy this file to .env.terraform and fill in the values
# Usage: source .env.terraform && terraform apply -var-file="staging.tfvars"

# Google OAuth (for Cognito SSO)
export TF_VAR_google_client_id=""
export TF_VAR_google_client_secret=""

# GitHub OAuth (for backend direct auth)
export TF_VAR_github_client_id=""
export TF_VAR_github_client_secret=""
